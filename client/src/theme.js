// In your theme.js or a similar file
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    fontFamily: "Lato, Arial, sans-serif",
  },
});

export default theme;
